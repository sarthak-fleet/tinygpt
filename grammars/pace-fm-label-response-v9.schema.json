{
  "type": "object",
  "additionalProperties": false,
  "required": ["spokenText", "pointAtLabel", "clickLabel", "bodyText"],
  "properties": {
    "spokenText": {
      "type": "string",
      "minLength": 2,
      "maxLength": 300
    },
    "pointAtLabel": {
      "type": "string",
      "description": "Empty string for 'no target'. Otherwise the natural-language label from the element list.",
      "maxLength": 100
    },
    "clickLabel": {
      "type": "string",
      "description": "Same shape as pointAtLabel. Empty string when no click is requested.",
      "maxLength": 100
    },
    "bodyText": {
      "type": "string",
      "description": "Body content for compose/draft intents (mail, message, note). Empty string when not composing.",
      "maxLength": 800
    }
  }
}
