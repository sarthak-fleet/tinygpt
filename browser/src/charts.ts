/**
 * charts.ts — training visualisation (Phase 4).
 *
 * STATUS: documented stub. No implementation yet.
 *
 * Renders, on the main thread, from TrainingProgress messages:
 *   - train loss vs step
 *   - val loss vs step
 *   - tokens/second
 *   - active backend (wasm / wasm-simd / webgpu)
 *
 * Guide: docs/browser_notes.md ("Web Worker")
 *
 * TODO(phase-4): minimal canvas/SVG line chart; no heavy chart dependency needed.
 */
export {};
